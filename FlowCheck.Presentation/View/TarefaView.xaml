<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FlowCheck.Presentation.View.TarefaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlowCheck.Presentation.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource Cinza12}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Margin="20,0,20,0" Padding="0" Background="{StaticResource Nenhuma}" CornerRadius="5">
            
            <TextBox x:Name="txtTituloTarefa" 
                     LostFocus="txtTituloTarefa_LostFocus" 
                     Visibility="Collapsed"  
                     VerticalAlignment="Center" 
                     Text="{Binding Titulo, Mode=TwoWay}" 
                     Foreground="{StaticResource Branco}" 
                     FontWeight="Bold" />
            
            <TextBlock x:Name="txbTituloTarefa" 
                       Text="{Binding Titulo}" 
                       DoubleTapped="txbTituloTarefa_DoubleTapped" 
                       Visibility="Visible" 
                       Padding="10,10,0,0" VerticalAlignment="Center"
                       Foreground="{StaticResource Branco}" 
                       FontWeight="Bold"/>
        </StackPanel>
        
        <!-- Botões de adicionar/deletar -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0,8,12,0">
            <Button Content="+" Width="32" Height="32" Margin="4"/>
            <Button Content="🗑️" Width="32" Height="32" Margin="4"/>
        </StackPanel>

        <!-- Lista de Tarefas -->
        <ScrollViewer Grid.Row="2" Margin="12,0,12,0">
            <StackPanel Spacing="8">
                <!-- Exemplo de tarefa -->
                <Border Background="#222" CornerRadius="6" Padding="10">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Width="24" Height="24" VerticalAlignment="Top"/>
                        <StackPanel Margin="10,0,0,0" >
                            <TextBlock Text="Potenti Mauris Bibendum Commodo Senectus Lacinia Risus Viverra"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="Descrição opcional da tarefa. Pode conter detalhes adicionais."
                                       FontSize="12"
                                       Foreground="Gray"
                                       TextWrapping="Wrap"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Right">
                            <Button Content="▲" Width="24" Height="24" Margin="4" />
                            <Button Content="X" Width="24" Height="24" Margin="4" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Repita mais tarefas aqui -->
            </StackPanel>
        </ScrollViewer>
        
    </Grid>
</Page>
